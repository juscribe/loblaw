# encoding: utf-8
require_dependency "loblaw/application_controller"

module Loblaw
  class GraphsController < ApplicationController

    # def index
    #   @voices = Voice.order(:id).limit(10).reverse_order.includes(:graph)
    #   @graphs = @voices.map(&:graph)
    # end
  end
end
